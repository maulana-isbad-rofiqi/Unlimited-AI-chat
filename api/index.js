// This is a placeholder for api/index.js. Since Vercel API routes are in separate files like api/token.js and api/chat.js, this file can be a router or empty.
export default function handler(req, res) {
  res.status(200).json({ message: 'API root' });
}
